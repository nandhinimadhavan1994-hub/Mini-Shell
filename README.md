# Mini-Shell
Developed a Mini Shell in C that simulates basic Linux shell functionality by executing user commands. The program uses system calls like fork(), exec(), and wait() to create processes and run commands in a command-line environment.
